---
site:
  title: Connectivity Link
  start_page: modules::index.adoc

content:
  sources:
    - url: .
      start_path: content

ui:
  bundle:
    url: https://github.com/rhpds/rhdp_showroom_theme/releases/download/latest/ui-bundle.zip
    snapshot: true

antora:
  extensions:
    - require: '@sntke/antora-mermaid-extension'
      mermaid_library_url: https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.esm.min.mjs
      script_stem: header-scripts
      mermaid_initialize_options:
        start_on_load: true
        # TUDO EM snake_case, inclusive o que e aninhado -- e isto custou duas
        # publicacoes. A extensao MINUSCULA as chaves antes de converter
        # snake_case -> camelCase, entao escrever camelCase aqui produz
        # 'usemaxwidth' e 'themevariables', que o mermaid ignora calado.
        # Conferido no HTML publicado nas duas tentativas.
        #
        # useMaxWidth: false tira o max-width que o mermaid crava a partir do
        # tamanho NATURAL do desenho, que e a causa de ele sair pequeno no meio
        # de uma coluna larga.
        flowchart:
          use_max_width: false
        # A fonte dimensiona o desenho INTEIRO: o mermaid calcula o tamanho dos
        # nos a partir do texto. Subir a fonte aumenta tudo proporcionalmente,
        # o que e diferente de esticar o SVG por CSS.
        theme_variables:
          font_size: 18px
    - require: '@andrew-jones/antora-tabs-extension'

output:
  dir: ./www
